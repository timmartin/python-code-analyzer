import * as React from "react";

import ObjectTree from "../src/ObjectTree";

export default {
  title: "Object tree",
  component: ObjectTree,
};

export const Default: React.FC = () => <ObjectTree />;
